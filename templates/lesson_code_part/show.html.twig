{% extends 'base.html.twig' %}

{% block title %}LessonCodePart{% endblock %}

{% block body %}
    <h1>LessonCodePart</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lesson_code_part.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ lesson_code_part.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lesson_code_part_index') }}">back to list</a>

    <a href="{{ path('app_lesson_code_part_edit', {'id': lesson_code_part.id}) }}">edit</a>

    {{ include('lesson_code_part/_delete_form.html.twig') }}
{% endblock %}
